import styled from "styled-components";

export const DisplayFilter = styled.div`
    display: flex;
    gap: 20px;
`
export const FilterDisplay = styled.span`
    display: flex;
    gap: 10px;
    padding: 12px 24px;
    cursor: pointer;
    font-size: 14px;
    border: none;
    border-radius: 35px;
    color: #202129;
    box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.25);
    outline: 1px dotted #808080;
    background: transparent;
`